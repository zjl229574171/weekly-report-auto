# 周报总流程（深文档 C）
文档性质：组员日常必读的流程规范。由 AI 启动指引（文档 B）调度到本文档；仓库内副本 weekly-report-auto/C_周报总流程.md。
流程版本：以 Git 仓库 tag / B 文档《AI启动指引》为准
维护者：流程发起人（腾讯文档在线版 policy=2 只读）
## 一、数据体系总览（先看懂这个）
《微波周报数据》Sheet 是唯一数据真相源——所有周报、月报都从它取数；每周 PPT 只是展示层。
你的对话 ──(AI 整理 + 你确认)──▶ Sheet「周报记录」+ 微云图片公开链                                        │              ┌─────────────────────────┤              ▼                         ▼     每周模板 PPT（展示层）        月报聚合（从「周报记录」取数，     写你名字那页                  不从 PPT 读）
Sheet 资源清单（恒定）

| 资源 | 值 | 说明 |
|---|---|---|
| Sheet 《微波周报数据》 file_id | WcPXBJrdsaWL | 唯一数据真相源 |
| Sheet URL | 见本地 manifest.json（由初始化文档生成） | 恒定 |
| 子表「周报记录」 sheet_id | HPUTdN | 每人每条工作一行 |
| 子表「项目词表」 sheet_id | BB08J2 | 项目归属，自动生长 |
| 子表「本周模板」 sheet_id | WQ6cAq | 各周期模板指针（动态  file_id  的唯一权威来源） |
| 样例页《周报个人页 · 标准样例》 | file_id WdkegYEAmUCz（URL 见 manifest.json） | 版式权威参照（发起人维护；组员只读不写） |
| 子表「花名册」 sheet_id | CM9sA2 | A 列 = 姓名，一行一人 |
| 子表「流程版本」 | （ sheet_id  以实际为准，用 get_sheet_info 按名查） | 流程版本号，启动协议用 |
| 母版《微波周报 · 母版（勿编辑）》 | file_id WAQPBCEcOnDw（URL 见 manifest.json） | 唯一克隆源，绝不修改；共  2  页：第  1  页 = 占位符页（ {{ 姓名 }}/{{ 周次 }} ，克隆源），第  2  页 = 标准样例页（格式说明，只读不写） |

各周期模板 file_id 是动态的，权威来源永远是「本周模板」子表，不要存死链接。
流程类云端文档统一收纳于「周报流程自动化」文件夹（文件夹 id WHisrjuWOgeI，URL 见 manifest.json；母版、各周期模板、Sheet《微波周报数据》、流程手册均在其中；新建流程文档一律入此文件夹）。
## 二、周期口径
- 周报每周四汇报；一个周期 = 上周五 ~ 本周四（共 7 天）。
- 周次格式（全系统唯一标准）：YYYY-MM-DD~YYYY-MM-DD，如 2026-08-21~2026-08-27 / 2026-08-28~2026-09-03。数据表与幻灯片 {{周次}} 占位符统一用此格式，人工补录也用此格式。
- 周期按当天自动推算（周五/六/日 → 下周四截止；周一~周四 → 本周四截止），无需手算。
- 每周模板由发起人侧云端自动化每周五 08:00 提前生成（当前+下一周期），组员开写时模板必然已存在，无需自建。
## 三、四步闭环（组员视角）
① 对话输入内容与图片        ↓② AI 整理对话内容 → 用户确认（不确认不写）        ↓③ 记录进 Sheet「周报记录」+ 图传微云拿公开链写进去        ↓④ 读 Sheet「本周模板」拿当周模板 → 写进模板里本人姓名那页
### Step① 对话输入
- 组员发文字（本周做了什么）+ 直接把图发到对话里
- 口头说明"这张是 XX 项目 / 讲什么"
- AI 先确认：周次、姓名（若组员没说）
- 本步 AI 不写任何东西，只接收
### Step② 整理 → 确认
- AI 按项目词表（第四节）把每条工作归到项目（未命中 → 问"归并还是新建"）
- 逐条问明时效（人力成本）：每条工作必须有时效（如 0.5天、2h、1天）——组员口述没带时效时，AI 在草稿确认环节主动逐条询问，不自行估算、不脑补；组员拒答则该条时效留空并标注"时效待补"
- 对每张图：问清"属于哪个项目/哪条工作 + 图注意什么"——图注必须由人给，AI 不脑补
- AI 输出结构草稿（含每条时效）让组员核对（不直接写库）
- 必须等组员回复"确认"后再进 Step③
### Step③ 记录 + 传图
A. 上传图到微云拿公开链
跨机规则：图传到本人微云账号的任意位置即可——公开链才是关键，图存在谁的网盘不重要。切勿套用他人 pdir_key（那是别人账号里的文件夹，会报错）。
三步上传（一次完整上传必须走完，预上传不会直接落文件）：
1. 调 weiyun.upload（预上传模式，不带 req_header；pdir_key 只传自己账号里的文件夹 key）→ 返回 file_id + upload_key + channel_list（仅预上传凭据，文件还没传上去）
2. 用返回的 upload_key 和分片信息继续调 weiyun.upload（分片上传，带 file_data）→ 分片全部传完文件才落库；秒传命中（file_exist=true）则跳过本步
3. 调 weiyun.gen_share_link → 拿 short_url
4. 验证：curl -s -o /dev/null -w "%{http_code}" -L <short_url> 应返回 200
5. 多张图时，「图片链接」列用分号 ; 分隔多个短链（如 abc123;def456）
6. 微云上传所需 SHA1 参数（file_sha / block_sha_list / check_sha / check_data）用本地脚本计算：python weekly-report-auto/_weiyun_params.py <图片路径>（脚本随 Git 仓库分发并已通过测试向量校验）
7. 大参数（图片 base64）或 array 字段（block_sha_list 等）走命令行超长/框架限制时：改用本地 Python 直调（import tencentdocs as td; td.call_tool() 同理适用微云 connector-proxy）。proxy 端口是动态的，从环境变量 CODEBUDDY_MCP_CONFIG 读取，不要写死。
B. 写进「周报记录」(HPUTdN)
- sheet.get_cell_data（数字索引start_row/end_row，不是 "A1:D14"）读末行定位下一空行
- sheet.set_range_value 追加一行，values 格式为 [{row, col, value_type:"STRING", string_value}] 列表，顺序（9 列）：周次 / 姓名 / 项目 / 类型 / 内容 / 时效 / 图片链接 / 图注 / 状态
- 状态填 待确认（组员后续在 Sheet 里改 已确认）
- 追加纪律：不与 Sheet 已有行做相似性比对，不提示"重复/合并"、不改写已有行——口述内容逐条如实追加；疑似重复、未完成、异常时效一律不拦截不询问，留给 Step④ 4 的 PPT 内容草稿确认时人工检查
写入模板时机：Step④（写 PPT）默认只在每周四执行并询问一次。其他任何日期——周五/六/日提前整理、周一~周三提前整理——默认只做 Step①~③（整理确认 + 写进「周报记录」+ 图传微云拿公开链），不写模板、不询问，除非用户主动要求。提前整理的内容先进 Sheet，周四再排版上模板。
### Step④ 写入本周公共模板
1. 读 Sheet「本周模板」子表（WQ6cAq）→ 找当周行 → 拿模板 file_id
2. 定位分区：模板页按花名册顺序排列，找到本人姓名页
3. 缺页兜底（本人姓名页不存在，通常是新人晚于模板生成加入）：
- slide_duplicate_slide 复制任意一页到末尾
- slide_find_replace_text 把该页姓名替换成新人姓名（版式原样保留）
- 提醒用户告知发起人在「花名册」子表加行
4. 先草稿后写入（写入必经流程链）：① 从 Sheet「周报记录」读本人本周全部行 → 按第六节标准样式整理 PPT 内容草稿（项目红小标题 + 同类归并条目 + 各项目总时效=各条相加）→ ② 发用户确认草稿；用户顺带检查归并结果（有无重复条目、总时效异常），发现问题指出后 AI 改 Sheet 明细再重新整理 → ③ 确认后读母版第 2 页《标准样例页》确定版式（样例异常时退回上一周期本人页，仅取其正确部分，与第六节标准样式冲突时以标准样式为准）→ ④ 写入本人页
5. 写入内容：按项目分组组织（铁律 2 的工程模块轴）——红小标题 #C00000 16pt =「一、项目名：总结提炼（时效 X天）」，时效=该项目各条时效相加；条目 #333333 13pt 不带时效，按语义归并：同一项目下语义相同/相近的工作合并为一条，不同任务保持分点（一个项目可有多个 • 点，不强制压缩成一句）；排版规则：两栏·目标驱动（细则见第六节）。版式页数按内容自适应，内容多可加页（插自己页之后、下一人页之前）。Sheet「周报记录」保留逐条明细与各自时效，展示页合并汇总，两层不冲突
6. 嵌图：以「周报记录」图片链接列为准——有公开链 → 下载图 upload_image(base64→image_id) → slide_add_image 嵌右栏 + 图注 11px；链接为空 → 默认无图，两栏排字铺满，不询问组员；大参数走 import tencentdocs as td; td.call_tool()（命令行传参会超 Windows 长度限制）
7. 绝不点"保存到我的文档"：直接编辑原文件（policy=3 所有人可编辑）；保存副本 = 新 file_id = 原模板空置 + 指针失效 + 月报缺数
8. 不用 WorkBuddy 直接网页手编自己页的人：内容不进「周报记录」，需事后口述给 AI 补录，否则月报聚合缺数
9. 完成必附链：每次写完/改完本人页，回复正文必须给出模板 URL + 定位（第 N 页·姓名），方便审查。
## 四、项目词表机制（初始为空，自动生长）
项目词表初始为空——不预设任何项目归属，避免硬编码出错。
运作规则：
1. 组员口述本周工作 → AI 语义推断归属项目
2. 命中词表：直接用词表里的项目名
3. 未命中：AI 问用户"归并到现有项目 X 还是新建？"——不瞎编、不脑补
4. 用户确认新建 → AI 把新项目写回「项目词表」（项目名/项目成员/备注；项目成员=该项目所有参与者，从组员口述中提取，多人用顿号分隔，如 钟佳良、李东琪）
5. 词表随使用自动生长，越来越准
词表是 AI 维护的，组员不需要手动编辑。但组员可以在 Sheet 里查看词表确认归属。
铁律：项目列一律填写词表里的规范项目名（一个项目一个名字，不搞别名）；AI 语义推断只能命中词表，未命中必须询问，不得自造变体/别名（如"微波标定"≠"微波载荷定标"）。
## 五、花名册（Sheet 动态维护，本文档不硬编码名单）
模板分区名单的唯一来源是 Sheet「花名册」子表（CM9sA2，A列=姓名，一行一人）。本文档不罗列名单——加人减人直接改 Sheet，永远以子表当前内容为准。
加人/减人规则：
- 加新人 → 发起人在「花名册」子表加一行姓名 → 下一次自动化运行建的模板自动带上
- 若模板已提前生成、新人本周就要写 → 见第三节 Step④ 的"缺页兜底"
- 减人 → 删行即可，已生成的历史模板不动（历史留档）
- 加页规则：在自己页之后、下一人页之前插入。多人共编时注意不要插错位置。
## 六、模板使用规范（组员侧）
模板的构建（克隆母版法）是发起人侧职责，见深文档 D《模板构建手册》，组员无需阅读。本节只讲使用。
- 幻灯片 16:9（960×540），每人一页：顶部姓名+周次，下面直接写工作
- 页序 = 花名册行序；无封面、无起始页
- 内容区不预置结构——版式权威 = 母版第 2 页《标准样例页》（母版 file_id WAQPBCEcOnDw，policy=2 组员只读；发起人维护）；标准样式如下：
- 红小标题 #C00000 16px 加粗：按项目分组，「一、项目名：总结提炼（时效 X天）」，时效为该项目各条相加
- 黑项目符号 #333333 13.5px：按语义归并——同类工作合并为一条，不同任务保持分点（一个项目可多个 • 点），不带时效
- 两栏排版（目标驱动）：内容区 x≈22~940，y≈100 起、可用到 y≈500；文字流式连续（项目按编号顺序先左栏后右栏，禁止两栏均分打断顺序，条目不拆散）；图填空（先文字后图、图放剩余空间不占文字区、图宽 180~400 自适应放大缩小）；行距随内容量（条目≤8→26~28pt / 9~14→24pt / ≥15→20pt）；写完后自检无大片空白、顺序连续、图不遮文字
- 右栏嵌图（x≈524, w≈400 基准）+ 图注 11px；周报图全部绘制不筛选；无图时内容区两栏铺满
- 共同模块（如 FY3H DPPS/MWRI）：各人各自写进自己分区，不设共同页
- 渐变标题条颜色：蓝 #1F4E79 → 红 #C00000（个人页不要改动头部样式）
## 七、铁律（不可破）
1. 不夸大、不补推论、不篡改原文
2. 组织轴是工程模块（项目）不是人；模块按项目词表平铺，不硬编码
3. 图随条目走：一张图必须属于某条具体工作；无图注标"待口述"，AI 不脑补图意；禁游离图
4. 在线协作刚需：产物走腾讯文档/Slides（全组共编）
5. 人员归属不推断：只取用户明确说的负责人；拿不准的交用户确认
6. 「周报记录」只收本周周期内的工作（类型=本周工作），不做完成度判断——进行中/未完成的工作照常记录；未开始的计划项（下周期要做的事）不收
7. 绝不修改母版：母版是唯一克隆源，只允许 copy_file（由发起人侧执行）
8. 对外 URL 一律用 API 返回的 url 字段：分享链接是 D+base64(file_id) 编码格式，拿原始 file_id 手拼 URL 打开会永远转圈
9. 新文件必须开权限：克隆/新建的文件默认 policy=0 私有，必须 manage.set_privilege(policy=3)，否则成员打不开
10. 不确认不写：未获用户"确认"前，AI 不写 Sheet、不写模板
11. 云端唯一产物：周报 PPT = 云端模板本人页。用户说"生成/写 PPT"一律指云端排版完成，禁止导出/下载为本地文件；仅当用户明确说"导出文件"时才走导出，且导出物只是副本预览，不是交付物
## 七A、对话展示纪律（默认静默）
默认静默执行：打开 Sheet、定位空行、写入记录、上传微云、替换占位符等工具执行细节不逐条播报，不展示"已打开 XX 表格""记录结果如下"这类过程节点。
只展示关键交互：① 草稿确认（两类：记录草稿——写 Sheet 前，如"周记录草稿如下，是否写入云 Sheet？"；PPT 内容草稿——Step④ 写模板前，见三节 Step④ 4）② 写入完成后的结果摘要（如"已记录 N 条，图片链接已填入"）③ 完成必附链（模板 URL + 第 N 页·姓名）④ 遇到问题/分歧/需你决策时才展开说明（时效待补、图注缺失、项目未命中、Sheet 数据异常、写库失败等）。
每轮尽量一次性给出"可确认内容 + 待决策项"，减少来回问答。
退出静默（调试用）：你在本轮对话开头说明"不要静默执行/展示过程细节"时，本次会话改为详细模式逐条播报，下轮自动恢复默认静默。
## 八、手动编辑 Sheet 注意事项
可以改：
- 自己行的「状态」列：待确认 → 已确认
- 自己行的文字错别字、图注
不要动：
- 表头行和列顺序（AI 按列位置写入，动了对不上）
- 别人的行
- 「本周模板」子表（自动化管理的指针）、「花名册」子表（发起人维护）、「流程版本」子表（发起人维护）
- 「图片链接」列贴本地路径或未公开的链接——必须是浏览器无登录也能打开的公开短链
追加新行：建议走 AI 对话补录（列序、周次格式、图片链、时效自动对齐）。手动追加必须严格按 9 列顺序：周次 / 姓名 / 项目 / 类型 / 内容 / 时效 / 图片链接 / 图注 / 状态，周次格式 YYYY-MM-DD~YYYY-MM-DD。
## 九、已知坑（踩过，避开）
1. weiyun.upload、weiyun.gen_share_link 都不能带 req_header（实测均报 additional properties 错误）；只有 weiyun.list 可以带
2. 微云上传预上传≠上传完成：预上传只返回 file_id+upload_key+channel_list，必须继续分片上传（带 file_data）文件才真正落库；秒传命中 file_exist=true 才可跳过
3. sheet.get_cell_data 必须用数字索引（start_row/start_col/end_row/end_col）。传 "A1:D14" 这类字符串**不报错但返回错误数据**（实测只返回表头，极易误判成功），更隐蔽
4. get_cell_data 返回 CSV 的键名是 csv_data，实测在 content[0].text（JSON 字符串）里，没有 structuredContent；解析用 json.loads(c0["text"])["csv_data"]，两种位置都要兼容
5. Sheet 返回大量纯逗号尾随占位行 ,,,,,，行数统计要过滤（l.strip(",") 为空才算空）
6. td.call_tool 返回 tuple (res, err)，必须解包，不能直接当对象用
7. 分享链可能过期 → 生成 PPT 时下载图内嵌优于只挂活链
8. 组员绝不点"保存到我的文档"：直接编辑原文件；保存副本=原模板空置+指针失效+月报缺数
9. slide_add_image 大参数（base64）走命令行会超长报错——错误信息因 shell 而异（Git Bash 报 "Argument list too long"，Windows 原生 CreateProcess 才报 WinError 206 ≤32767 字符）；结论一致：大参数一律用 import tencentdocs as td; td.call_tool() 绕开
10. slide-mcp 删页工具名是 slide_remove_slide（不是 slide_delete_page）；删 shape 用 slide_remove_shapes
11. URL 手拼陷阱：分享 URL = D + base64(file_id) 编码格式；拿原始 file_id 手拼分享 URL 打开会永远转圈。一律用 API 返回的 url 字段
12. 新文件默认私有：copy_file/create_file 产物 policy=0（仅创建者），必须补 manage.set_privilege(policy=3) 才能全组打开
13. slide_get_info 的页数字段是 slide_count（不是 page_count）；slide_find_replace_text 一次只处理一页（page_index 从 0 起），必须逐页调用
14. manage.copy_file 返回的键名是 id（manage.create_file 返回的是 file_id），两者不一致
15. 周模板文件所有者是发起人账号：统一管理；发起人长期不在岗需网页端转让所有权
16. slide_add_text 不支持 bold 参数（schema 无该字段，传了会报错/静默失败）；需要加粗的标题先 add_text 再补 slide_set_text_property(shape_id, bold=True)
## 十、组员开场白（直接复制用）
请读本地 weekly-report-auto/C_周报总流程.md（若无本地副本，先读启动指引文档完成 git clone 初始化），按四步路径帮我写本周周报。1）我先口述本周工作并直接把图发给你；2）你整理成结构草稿让我确认（项目按词表归，每条工作问清时效/人力成本如 0.5天、2h，图问清归属和图注）；3）我确认后，你把记录写进《微波周报数据》Sheet 的「周报记录」，并把图上传到我的微云拿公开链接写进去；4）最后读「本周模板」子表拿当周公共模板，把我的内容写进模板里我名字那一页（若没有我名字那页，复制一页并把姓名替换成我）。现在开始，本周周期是上周五到本周四（如 2026-08-28~2026-09-03），我是 <你的名字>。

