# 模板构建手册（深文档 D）
> **权威在 Git 仓库**（本文档）。腾讯文档 file_id DV1p0Z0xIVERSU095 由 `sync_de.py` 从本文档同步（云端自动化 prompt 引用腾讯文档，无 git；改本文档后必须运行 `python3 sync_de.py D`）。
文档性质：发起人 / 云端自动化专用。周模板的构建（克隆母版法）是发起人侧一次性/自动化职责，组员一般不使用本文档；组员侧唯一相关操作（缺页兜底）已内联在 C 文档 Step④，无需读本文。
流程版本：以 Sheet「流程版本」/ B 文档《AI启动指引》为准维护者：流程发起人（policy=2 只读；本文档同时挂在云端自动化 prompt 的引用链中）
## 一、职责划分

| 角色 | 职责 |
|---|---|
| 云端自动化（ WorkBuddy  项目级， ID 6553649 ，每周五  08:00 ） | 自动确保【当前周期  +  下一周期】模板就绪（幂等），纯  MCP  原生，只绑  tdocs-app  连接器，不依赖任何人本机在线 |
| 发起人 | 手动补建模板（照本手册执行，任何设备 / 会话均可）、维护花名册、头几周核对自动化产出 |
| 组员 | 只 ** 使用 ** 模板（写自己名字那页），不构建。使用规范见  C  文档 |

绝不修改/删除母版（file_id WAQPBCEcOnDw，标题"微波周报·母版（勿编辑）"，policy=2 只读保护）——它是所有模板的唯一克隆源。
## 二、为什么用"克隆母版"而不是从零搭建
1. 克隆是像素级复制，渐变标题条、字体、加粗等版式细节 100% 保留（MCP 接口无渐变字段，从零搭建会失真）
2. 全流程只调腾讯文档官方 MCP 工具（copy_file / slide_duplicate_slide / slide_find_replace_text / set_privilege），不依赖本机 Python、脚本、路径、端口
3. 每周模板 = 母版克隆 + 复制页 + 替换占位符三步完成，稳定可复现
4. 克隆产物 policy=3 所有人可编辑，组员点链接直接改
## 三、周期与生成时机
- 周报每周四汇报，周期 = 上周五 ~ 本周四（7 天）；周次格式 YYYY-MM-DD~YYYY-MM-DD
- 每次运行确保【当前周期 + 下一周期】两份模板都已登记（幂等）
- 定时任务每周五 08:00 跑 → 下一周期模板提前 7 天就绪，组员开写时模板必然存在，无并发/缺模板冲突
- 建议头几周每周五手动核对一次产出（Sheet 登记 + 打开模板抽查），稳定后放手
## 四、克隆法生成步骤（自动化内部逻辑；手动补建照此执行）
1. 幂等查重：get_cell_data 读「本周模板」WQ6cAq，该周次已登记（file_id 非空）→ 跳过
2. 读名单：get_cell_data 读「花名册」CM9sA2 A列 → N 人名单（顺序=页序）
3. 克隆母版：manage.copy_file(file_id=WAQPBCEcOnDw, title=微波周报{周期}) → 返回键名 id
4. 扩页：循环 N-1 次 slide_duplicate_slide(page_indexes=[0])，只复制母版第 1 页（占位符页，slide_ids[0]）；母版第 2 页是《标准样例页》，绝不复制；直到 slide_count==N+1（克隆体含克隆源+扩页，样例页不入模板）——克隆完成后若 slide_count!=N+1 或克隆体内出现"标准样例页"文本 → 母版结构异常，delete_file 删半成品并报告
5. 替换：逐页 slide_find_replace_text（一次只处理一页，page_index 从 0 起，仅前 N 页）：
- {{姓名}} → 花名册第 i 人
- {{周次}} → 周次数字串（YYYY-MM-DD~YYYY-MM-DD，与数据表一致）
- 每次校验返回 total_replacements≥1；为 0 = 母版损坏 → delete_file 删半成品并报告
6. 开权限：manage.set_privilege(file_id, policy=3)
7. 登记：set_range_value 写「本周模板」第一个空行：周次 | 模板URL(API返回的url字段) | file_id | 使用中 | 克隆法v5
要点：
- 母版共 2 页：第 1 页=占位符页（克隆源），含 {{姓名}}（白色 28px 微软雅黑加粗）、微波周报 · {{周次}}（白色 15px），顶部蓝 #1F4E79 → 红 #C00000 渐变标题条；第 2 页=《标准样例页》（版式权威参照，含格式说明文字），仅作组员阅读，绝不克隆进模板
- 母版绝不改版式；要改版式（含样例页更新）→ 发起人在母版上改（需临时提权 policy=3，改完必须恢复 policy=2）→ 升小版本号
- 登记时 URL 一律用 API 返回的 url 字段（勿手拼，见 C 文档已知坑 11）
- 半成品（克隆成功但替换/开权限失败）必须 delete_file 清理后重试，不留脏文件；已登记行对应的模板文件绝不 delete_file/覆盖/改写——组员可能已填写内容，delete_file 仅限本次新建的半成品
## 五、维护花名册
- 花名册在 Sheet「花名册」子表（CM9sA2，A列=姓名，一行一人），加新人只需加一行，下次自动化运行自动生效
- 减人删行即可；已生成的历史模板不动（历史留档）
- 模板已生成、新人本周就要写 → 组员侧按 C 文档 Step④"缺页兜底"自行补页
## 六、维护项目词表
- 词表初始为空，随组员使用自动生长（规则见 C 文档第四节）
- 发起人定期审查花名册与项目词表，清理重复/错误条目
## 七、维护「流程版本」子表
- Sheet《微波周报数据》中新建子表「流程版本」，列结构：版本 / 是否强制 / 更新说明 / 时间
- 任何对 A/B/C/D/E 五份文档或附录代码的修改，都必须在此子表更新版本号：
- 内容修正、增补说明 → 小版本 +0.1（如 5.2 → 5.3），是否强制=否
- 协议变更、列结构变更、不兼容改动 → 大版本 +1（如 5 → 6），是否强制=是
- 同步更新 B 文档头部版本号；不一致时以本子表为权威
## 七A、URL 恒定原则（版本更新铁律）
腾讯文档的 URL 由文件身份决定：文件不删，链接永不变。文档内互链（B→C/D/E/Sheet、manifest 五链接、自动化 prompt 引用）全部依赖 URL 稳定，因此任何版本更新一律原地编辑，绝不重传新文件：
- 小改动（条款措辞、版本号、增补说明）：用 find + replace_text / find_and_replace 原地替换
- 大改动（结构性重写）：用 overwrite_doc_with_html 整篇覆盖（同一 file_id，旧版本自动进历史可回溯）；附录代码先以占位符占位，再用 insert_code_block 补插原生代码块
- 严禁「新建文档再改链接」的更新方式——那会使所有引用该文档的链接失效，引发全链路连锁更新
## 八、已知坑
继承 C 文档第九节全部 15 条（本文档执行者同样适用），另加：
16. 自动化 prompt 修改后，先手动触发一次验证幂等查重逻辑（不应产生重复模板行），再恢复定时
17. 云端自动化运行环境没有本地文件系统可依赖——prompt 中不要引用本地路径或本地脚本，一切通过 MCP 工具完成

